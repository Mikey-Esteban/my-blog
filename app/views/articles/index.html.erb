<h1> Listing Articles </h1>

<ul id="articles">
  <% @articles.each do |a| %>
    <li>
      <p>TITLE: <%= link_to a.title, article_path(a) %></p>
    </li>
  <% end %>
</ul>

<br>
<% if logged_in? %>
  <%= link_to 'Create New Article', new_article_path, class: 'new_article' %>
<% end %>
